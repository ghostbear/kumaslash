import me.ghostbear.common.logging.Log;

CREATE TABLE IF NOT EXISTS Logging (
    log TEXT AS Log NOT NULL,
    guildId INTEGER NOT NULL,
    channelId INTEGER NOT NULL,
    PRIMARY KEY (log, guildId, channelId)
);